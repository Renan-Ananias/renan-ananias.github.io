# Portfolio No-Build (Portfolio Host)

Versão **100% no-build** do portfolio — React via CDN, Babel Standalone compila JSX no navegador, Tailwind via CDN. **Sem npm, sem Vite, sem TypeScript, sem build step**.